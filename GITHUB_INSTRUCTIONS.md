# 📤 Инструкция по обновлению проекта в GitHub

## ✅ Обновление README

README.md уже обновлен и готов! Он включает:
- Описание проекта
- Инструкции по установке и запуску
- Структуру проекта с новыми компонентами
- Информацию об анимациях и cookies
- Инструкции по деплою

## 🚀 Как обновить проект в GitHub

### Вариант 1: Если репозиторий уже существует

Откройте **PowerShell** или **Git Bash** в папке проекта и выполните:

```powershell
# 1. Перейдите в папку проекта (если не там)
cd "C:\Users\Lev\Desktop\Проекты\corporate website"

# 2. Проверьте статус
git status

# 3. Добавьте все изменения
git add .

# 4. Создайте коммит
git commit -m "Update: Add animations, cookie banner, new components, fix README"

# 5. Отправьте на GitHub
git push
```

### Вариант 2: Если репозиторий еще не создан

#### Шаг 1: Создайте репозиторий на GitHub
1. Перейдите на https://github.com
2. Нажмите "New repository" (или "+" → "New repository")
3. Название: `corporate-website` (или любое другое)
4. Выберите **Public** или **Private**
5. НЕ ставьте галочки на "Initialize with README", "Add .gitignore", "Choose a license"
6. Нажмите "Create repository"

#### Шаг 2: Инициализируйте Git в проекте

```powershell
# 1. Перейдите в папку проекта
cd "C:\Users\Lev\Desktop\Проекты\corporate website"

# 2. Инициализируйте Git (если еще не сделано)
git init

# 3. Добавьте все файлы
git add .

# 4. Создайте первый коммит
git commit -m "Initial commit: Nomad Pays corporate website with animations and cookie banner"

# 5. Переименуйте ветку в main (если нужно)
git branch -M main

# 6. Подключите удаленный репозиторий (ЗАМЕНИТЕ URL на свой!)
git remote add origin https://github.com/ВАШ-USERNAME/corporate-website.git

# 7. Отправьте код на GitHub
git push -u origin main
```

**⚠️ ВАЖНО:** Замените `ВАШ-USERNAME` на ваш GitHub username и `corporate-website` на название вашего репозитория!

### Вариант 3: Если есть конфликты

```powershell
# 1. Получите изменения с GitHub
git pull origin main --allow-unrelated-histories

# 2. Разрешите конфликты, если они есть, затем:
git add .
git commit -m "Merge remote changes"
git push
```

## 📋 Что было добавлено/изменено

### Новые компоненты:
- ✅ `components/AnimatedIcons.tsx` - анимированные SVG иконки (лупа, график, глобус)
- ✅ `components/HowItWorksSection.tsx` - анимированная секция "Как работает"
- ✅ `components/WhySection.tsx` - анимированная секция "Почему Nomad Pays"
- ✅ `components/CookieBanner.tsx` - баннер согласия на cookies

### Обновления:
- ✅ `app/[locale]/page.tsx` - использует новые компоненты с анимациями
- ✅ `components/CookieBanner.tsx` - поддержка HTTP cookies + localStorage
- ✅ `messages/ru.json` и `messages/en.json` - добавлены переводы для cookie banner
- ✅ `README.md` - полностью обновлен с актуальной информацией

### Анимации:
- ✅ Анимация цифр (01-04) с поворотом и масштабированием
- ✅ Анимированные линии между элементами
- ✅ Плавное появление заголовков и описаний
- ✅ Анимированные SVG иконки в секции "Почему Nomad Pays"

## 🔍 Проверка перед коммитом

Убедитесь, что:
- ✅ Все файлы сохранены
- ✅ Нет ошибок в консоли (`npm run lint`)
- ✅ Проект запускается (`npm run dev`)
- ✅ README.md актуален

## 💡 Полезные команды Git

```powershell
# Просмотр изменений
git status

# Просмотр разницы
git diff

# Просмотр истории коммитов
git log

# Отмена изменений в файле (ОСТОРОЖНО!)
git checkout -- имя_файла

# Просмотр удаленных репозиториев
git remote -v
```

## 📝 Примеры сообщений коммитов

```
"Add: Animated icons and sections with Framer Motion"
"Update: Cookie banner with HTTP cookies support"
"Fix: README merge conflicts and update structure"
"Refactor: Split animated sections into separate components"
"Update: Component order in Why section (lens, globe, chart)"
```

## ⚠️ Если что-то пошло не так

### Проблема: "fatal: not a git repository"
**Решение:** Убедитесь, что вы в правильной папке проекта и выполните `git init`

### Проблема: "remote origin already exists"
**Решение:** 
```powershell
git remote remove origin
git remote add origin https://github.com/ВАШ-USERNAME/НАЗВАНИЕ-РЕПО.git
```

### Проблема: "failed to push some refs"
**Решение:**
```powershell
git pull origin main --allow-unrelated-histories
# Разрешите конфликты, если есть
git push
```

### Проблема: Git инициализировался в неправильной папке
**Решение:**
```powershell
# Удалите .git из неправильной папки (например, из домашней директории)
Remove-Item -Path "C:\Users\Lev\.git" -Recurse -Force -ErrorAction SilentlyContinue

# Перейдите в папку проекта
cd "C:\Users\Lev\Desktop\Проекты\corporate website"

# Инициализируйте Git правильно
git init
```

## 🎯 Быстрая команда для обновления

Если репозиторий уже настроен, используйте эту команду:

```powershell
git add . ; git commit -m "Update project" ; git push
```

---

**✅ Готово!** После выполнения команд ваш код будет на GitHub!
