# 📧 Настройка отправки email (Resend)

Форма контактов для рекламодателей **уже активирована** и готова к отправке email на `ads@nomadplatforms.co.uk`.

## ✅ Что уже сделано:

1. ✅ Пакет `resend` установлен
2. ✅ Код отправки email активирован
3. ✅ Форма настроена на отправку на `ads@nomadplatforms.co.uk`

## 🔑 Что нужно сделать:

### Для локальной разработки:

Создайте файл `.env.local` в корне проекта:

```env
RESEND_API_KEY=re_ваш_api_ключ_здесь
RESEND_FROM_EMAIL=onboarding@resend.dev
```

### Для продакшена (Vercel):

1. Перейдите в **Vercel Dashboard** → ваш проект → **Settings** → **Environment Variables**
2. Добавьте переменные:
   - **Name**: `RESEND_API_KEY`
   - **Value**: ваш API ключ из Resend (начинается с `re_`)
   - **Environment**: ✅ Production, ✅ Preview, ✅ Development

   - **Name**: `RESEND_FROM_EMAIL` (опционально)
   - **Value**: `noreply@nomadplatforms.co.uk` (если домен верифицирован)
   - **Environment**: ✅ Production, ✅ Preview, ✅ Development

## 🚀 Как получить API ключ Resend:

1. Зайдите на [resend.com](https://resend.com)
2. Зарегистрируйтесь или войдите
3. Перейдите в **API Keys**
4. Нажмите **"Create API Key"**
5. Скопируйте ключ (начинается с `re_`)
6. Добавьте его в переменные окружения

## 📝 Важно:

- **В режиме разработки** (`npm run dev`): если `RESEND_API_KEY` не установлен, форма будет работать, но не будет отправлять email (только логирование)
- **В продакшене**: без `RESEND_API_KEY` форма вернет ошибку при отправке

## ✅ После настройки:

1. Заполните форму на странице `/for-advertisers`
2. Нажмите "Отправить"
3. Проверьте почту `ads@nomadplatforms.co.uk` — должно прийти письмо с данными заявки

## 🔍 Проверка работы:

- Логи в консоли (разработка): `console.log("Email sent successfully...")`
- Логи в Vercel: **Deployments** → выберите деплой → **Functions** → `api/contact`

---

**Готово! Форма готова к реальной отправке email! 🎉**
