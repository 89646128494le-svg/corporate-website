# 📘 Инструкция по деплою на Vercel

Пошаговое руководство по размещению сайта Nomad Pays на Vercel с подключением GitHub и настройкой домена.

## 📋 Что включено в эту инструкцию:

1. **Деплой на Vercel** — полная настройка проекта на Vercel
2. **Подключение домена** — настройка DNS и SSL сертификата
3. **Настройка отправки email** (Шаг 6) — подробная инструкция по настройке Resend для отправки сообщений из формы на `ads@nomadplatforms.co.uk`
4. **Настройка кнопок App Store/Google Play** (Шаг 7) — как сделать кнопки рабочими, где взять ссылки, как их менять

**Быстрый старт:**
- Нужно настроить email? → Смотрите **Шаг 6**
- Нужно настроить кнопки приложений? → Смотрите **Шаг 7**

---

## 🚀 Шаг 1: Подготовка проекта

### 1.1. Убедитесь, что проект готов к деплою

```bash
# Проверьте, что проект собирается без ошибок
npm run build

# Если есть ошибки, исправьте их перед деплоем
```

### 1.2. Убедитесь, что все изменения закоммичены в Git

```bash
git status
git add .
git commit -m "Ready for deployment"
```

---

## 📦 Шаг 2: Загрузка проекта на GitHub

### 2.1. Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите кнопку **"New"** (или **"+" → "New repository"**)
3. Заполните данные:
   - **Repository name**: `nomad-pays-website` (или любое другое название)
   - **Description**: "Official corporate website for Nomad Pays"
   - **Visibility**: Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

### 2.2. Загрузите код на GitHub

```bash
# Если репозиторий еще не инициализирован
git init
git branch -M main

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Nomad Pays corporate website"

# Подключите удаленный репозиторий (ЗАМЕНИТЕ URL на свой!)
git remote add origin https://github.com/ВАШ-USERNAME/nomad-pays-website.git

# Отправьте код на GitHub
git push -u origin main
```

**⚠️ ВАЖНО:** Замените `ВАШ-USERNAME` на ваш GitHub username и `nomad-pays-website` на название вашего репозитория!

---

## 🌐 Шаг 3: Деплой на Vercel

### 3.1. Вход в Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** (или **"Log In"**, если уже зарегистрированы)
3. Выберите **"Continue with GitHub"** для входа через GitHub

### 3.2. Импорт проекта

1. После входа нажмите **"Add New..." → "Project"**
2. В разделе **"Import Git Repository"** найдите ваш репозиторий `nomad-pays-website`
3. Если репозитория не видно, нажмите **"Adjust GitHub App Permissions"** и разрешите доступ к нужным репозиториям
4. Нажмите **"Import"**

### 3.3. Настройка проекта

1. **Project Name**: Оставьте предложенное название или измените на `nomad-pays-website`
2. **Framework Preset**: Vercel должен автоматически определить **Next.js**
3. **Root Directory**: Оставьте `.` (корневая директория)
4. **Build Command**: Оставьте `npm run build` (по умолчанию)
5. **Output Directory**: Оставьте `.next` (по умолчанию)
6. **Install Command**: Оставьте `npm install` (по умолчанию)

### 3.4. Environment Variables (переменные окружения)

Если у вас есть переменные окружения (например, для аналитики):

1. Нажмите **"Environment Variables"**
2. Добавьте переменные:
   - `NEXT_PUBLIC_YANDEX_METRICA_ID` = `ваш_id_счетчика`
   - `NEXT_PUBLIC_GOOGLE_ANALYTICS_ID` = `G-XXXXXXXXXX`
   - `RESEND_API_KEY` = `ваш_api_ключ` (если используется Resend для отправки email)
3. Нажмите **"Deploy"**

### 3.5. Ожидание деплоя

1. Vercel начнет сборку проекта (займет 1-3 минуты)
2. После успешной сборки вы увидите **"Congratulations! Your deployment is ready"**
3. Нажмите **"Visit"** для просмотра сайта

**Ваш сайт будет доступен по адресу:** `https://nomad-pays-website.vercel.app` (или другое имя)

---

## 🔗 Шаг 4: Подключение домена

### 4.1. Добавление домена в Vercel

1. В Dashboard проекта нажмите на вкладку **"Settings"**
2. Перейдите в раздел **"Domains"**
3. В поле **"Domain"** введите ваш домен (например, `nomadpays.com` или `www.nomadpays.com`)
4. Нажмите **"Add"**

### 4.2. Настройка DNS записей

Vercel покажет вам необходимые DNS записи. Выберите один из вариантов:

#### Вариант A: Поддомен (рекомендуется для тестирования)
Если вы хотите использовать поддомен (например, `www.nomadpays.com`):

1. **Тип записи**: `CNAME`
2. **Имя**: `www` (или другой поддомен)
3. **Значение**: `cname.vercel-dns.com`

#### Вариант B: Корневой домен
Если вы хотите использовать корневой домен (`nomadpays.com`):

1. **Тип записи**: `A`
2. **Имя**: `@` (или оставьте пустым)
3. **Значение**: IP-адрес, указанный Vercel (обычно `76.76.21.21`)

**Примечание**: Vercel также может предложить использовать `ALIAS` или `ANAME` записи — следуйте инструкциям Vercel.

### 4.3. Настройка DNS у регистратора домена

1. Войдите в панель управления вашего регистратора домена (GoDaddy, Namecheap, Cloudflare и т.д.)
2. Перейдите в раздел **"DNS Management"** или **"Управление DNS"**
3. Добавьте запись типа, указанного Vercel (CNAME или A)
4. Сохраните изменения

### 4.4. Ожидание распространения DNS

- **Поддомен (CNAME)**: 1-5 минут
- **Корневой домен (A)**: 5-30 минут, иногда до 48 часов

После распространения DNS ваш домен будет автоматически привязан к Vercel.

### 4.5. Проверка SSL сертификата

Vercel автоматически выпускает SSL сертификат для вашего домена через Let's Encrypt. Проверка обычно занимает несколько минут после привязки домена.

---

## ⚙️ Шаг 5: Дополнительные настройки

### 5.1. Настройка редиректов (если нужно)

Если у вас есть старый сайт и нужно настроить редиректы, добавьте в `next.config.js`:

```javascript
const nextConfig = {
  async redirects() {
    return [
      {
        source: '/old-page',
        destination: '/new-page',
        permanent: true,
      },
    ];
  },
};
```

### 5.2. Настройка автоматического деплоя

По умолчанию Vercel автоматически деплоит каждое изменение в ветке `main` (или `master`). Чтобы отключить это:

1. Перейдите в **Settings → Git**
2. Отключите **"Automatic deployments from Git"**

### 5.3. Preview deployments

Vercel автоматически создает preview deployments для каждого Pull Request. Это позволяет тестировать изменения перед мерджем в основную ветку.

---

## 🔧 Шаг 6: Настройка отправки email через Resend

Полная инструкция по настройке отправки сообщений из формы контактов на email `ads@nomadplatforms.co.uk`.

### 6.1. Регистрация в Resend

1. Перейдите на [resend.com](https://resend.com)
2. Нажмите **"Sign Up"** (или **"Log In"**, если уже зарегистрированы)
3. Зарегистрируйтесь через GitHub, Google или Email
4. После входа вы попадете в Dashboard

### 6.2. Получение API ключа

1. В Dashboard Resend перейдите в раздел **"API Keys"** (в левом меню)
2. Нажмите кнопку **"Create API Key"**
3. Введите название ключа (например, `nomad-pays-website`)
4. Выберите права доступа: **"Full Access"** (или **"Sending Access"**)
5. Нажмите **"Add"**
6. **ВАЖНО**: Скопируйте API ключ сразу! Он показывается только один раз и выглядит как `re_xxxxxxxxxxxxx`
7. Сохраните ключ в безопасном месте

### 6.3. Верификация домена (опционально, но рекомендуется)

Для отправки с вашего домена нужно его верифицировать:

1. В Dashboard Resend перейдите в **"Domains"**
2. Нажмите **"Add Domain"**
3. Введите ваш домен (например, `nomadplatforms.co.uk`)
4. Resend покажет DNS записи, которые нужно добавить:
   - **TXT запись** для верификации
   - **DKIM записи** для подписи писем
5. Добавьте эти записи в DNS вашего домена (у регистратора)
6. Подождите 5-30 минут для верификации
7. После верификации вы сможете отправлять письма с адресов вида `noreply@nomadplatforms.co.uk`

**Примечание**: Если домен не верифицирован, можно отправлять с адреса `onboarding@resend.dev` (для тестирования).

### 6.4. Установка пакета Resend

В корневой папке проекта выполните:

```bash
npm install resend
```

После установки пакет будет добавлен в `package.json`.

### 6.5. Добавление API ключа в Vercel

1. В Vercel Dashboard откройте ваш проект
2. Перейдите в **Settings → Environment Variables**
3. Нажмите **"Add New"**
4. Заполните:
   - **Name**: `RESEND_API_KEY`
   - **Value**: вставьте ваш API ключ из Resend (начинается с `re_`)
   - **Environment**: отметьте все три:
     - ✅ Production
     - ✅ Preview
     - ✅ Development
5. Нажмите **"Save"**

### 6.6. Обновление API Route для отправки email

Откройте файл `app/api/contact/route.ts` и замените весь код на следующий:

```typescript
import { NextRequest, NextResponse } from "next/server";
import { Resend } from "resend";

// Инициализация Resend с API ключом из переменных окружения
const resend = new Resend(process.env.RESEND_API_KEY);

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { name, phone, email, message } = body;

    // Валидация
    if (!name || !phone || !email || !message) {
      return NextResponse.json(
        { error: "Все поля обязательны для заполнения" },
        { status: 400 }
      );
    }

    // Email валидация
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(email)) {
      return NextResponse.json(
        { error: "Некорректный email адрес" },
        { status: 400 }
      );
    }

    // Отправка email через Resend
    try {
      const { data, error } = await resend.emails.send({
        from: process.env.RESEND_FROM_EMAIL || 'onboarding@resend.dev', // Используйте ваш верифицированный домен
        to: 'ads@nomadplatforms.co.uk', // Адрес получателя
        subject: `Новая заявка от рекламодателя: ${name}`,
        html: `
          <!DOCTYPE html>
          <html>
            <head>
              <meta charset="utf-8">
              <style>
                body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
                .container { max-width: 600px; margin: 0 auto; padding: 20px; }
                .header { background-color: #1e40af; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
                .content { background-color: #f9fafb; padding: 20px; border: 1px solid #e5e7eb; }
                .field { margin-bottom: 15px; }
                .label { font-weight: bold; color: #1e40af; }
                .value { margin-top: 5px; padding: 10px; background-color: white; border-radius: 4px; }
                .footer { background-color: #f3f4f6; padding: 15px; text-align: center; font-size: 12px; color: #6b7280; border-radius: 0 0 8px 8px; }
              </style>
            </head>
            <body>
              <div class="container">
                <div class="header">
                  <h2>Новая заявка от рекламодателя</h2>
                </div>
                <div class="content">
                  <div class="field">
                    <div class="label">Имя:</div>
                    <div class="value">${name}</div>
                  </div>
                  <div class="field">
                    <div class="label">Телефон:</div>
                    <div class="value">${phone}</div>
                  </div>
                  <div class="field">
                    <div class="label">Email:</div>
                    <div class="value">${email}</div>
                  </div>
                  <div class="field">
                    <div class="label">Сообщение:</div>
                    <div class="value">${message.replace(/\n/g, '<br>')}</div>
                  </div>
                </div>
                <div class="footer">
                  <p>Это автоматическое сообщение с сайта Nomad Pays</p>
                  <p>Время отправки: ${new Date().toLocaleString('ru-RU')}</p>
                </div>
              </div>
            </body>
          </html>
        `,
        replyTo: email, // Чтобы можно было ответить напрямую отправителю
      });

      if (error) {
        console.error("Resend error:", error);
        return NextResponse.json(
          { error: "Ошибка при отправке email" },
          { status: 500 }
        );
      }

      console.log("Email sent successfully:", data);
    } catch (emailError) {
      console.error("Error sending email:", emailError);
      return NextResponse.json(
        { error: "Ошибка при отправке email" },
        { status: 500 }
      );
    }

    return NextResponse.json(
      { message: "Сообщение успешно отправлено" },
      { status: 200 }
    );
  } catch (error) {
    console.error("Error processing contact form:", error);
    return NextResponse.json(
      { error: "Ошибка при обработке запроса" },
      { status: 500 }
    );
  }
}
```

### 6.7. Настройка адреса отправителя (опционально)

Если вы верифицировали домен, добавьте еще одну переменную окружения в Vercel:

1. В **Settings → Environment Variables** добавьте:
   - **Name**: `RESEND_FROM_EMAIL`
   - **Value**: `noreply@nomadplatforms.co.uk` (или другой адрес с вашего домена)
   - **Environment**: Production, Preview, Development

### 6.8. Тестирование отправки

1. Закоммитьте изменения:
```bash
git add .
git commit -m "Add Resend email integration"
git push
```

2. После деплоя на Vercel:
   - Откройте страницу `/for-advertisers` на вашем сайте
   - Заполните форму контактов
   - Отправьте сообщение
   - Проверьте почту `ads@nomadplatforms.co.uk` — должно прийти письмо

3. Если письма не приходят:
   - Проверьте логи в Vercel Dashboard → **Deployments** → выберите последний деплой → **Functions** → `api/contact`
   - Убедитесь, что `RESEND_API_KEY` правильно добавлен в Environment Variables
   - Проверьте, что адрес получателя правильный

### 6.9. Альтернатива: Nodemailer (если не хотите использовать Resend)

Если предпочитаете Nodemailer:

1. Установите пакет:
```bash
npm install nodemailer
npm install --save-dev @types/nodemailer
```

2. Настройте SMTP (Gmail, SendGrid, Mailgun и т.д.)
3. Обновите `app/api/contact/route.ts` с кодом Nodemailer

**Рекомендация**: Resend проще в настройке и лучше работает с Vercel.

---

## 📱 Шаг 7: Настройка кнопок App Store и Google Play

Полная инструкция по настройке рабочих ссылок на приложения в магазинах.

### 7.1. Получение ссылок на приложения

#### Для App Store (iOS):

1. Откройте [App Store Connect](https://appstoreconnect.apple.com)
2. Войдите в свой аккаунт разработчика
3. Выберите ваше приложение **Nomad Pays**
4. Перейдите в раздел **"App Information"**
5. Найдите **"App Store URL"** — это будет ссылка вида:
   ```
   https://apps.apple.com/app/nomad-pays/idXXXXXXXXXX
   ```
6. Скопируйте эту ссылку

**Альтернативный способ**: Если приложение уже опубликовано:
- Откройте App Store на iPhone/iPad
- Найдите приложение **Nomad Pays**
- Нажмите на кнопку **"Поделиться"** → **"Скопировать ссылку"**

#### Для Google Play (Android):

1. Откройте [Google Play Console](https://play.google.com/console)
2. Войдите в свой аккаунт разработчика
3. Выберите ваше приложение **Nomad Pays**
4. Перейдите в раздел **"Store presence" → "Main store listing"**
5. Прокрутите вниз до раздела **"App links"**
6. Найдите **"Store listing URL"** — это будет ссылка вида:
   ```
   https://play.google.com/store/apps/details?id=com.nomadpays.app
   ```
7. Скопируйте эту ссылку

**Альтернативный способ**: Если приложение уже опубликовано:
- Откройте Google Play на Android устройстве
- Найдите приложение **Nomad Pays**
- Нажмите на три точки (⋮) → **"Поделиться"** → **"Скопировать ссылку"**

### 7.2. Обновление компонента AppStoreBadges

Откройте файл `components/AppStoreBadges.tsx` и замените ссылки:

```typescript
"use client";

import Link from "next/link";
import { type Locale } from "@/lib/i18n";

interface AppStoreBadgesProps {
  locale: Locale;
  className?: string;
}

export default function AppStoreBadges({ locale, className = "" }: AppStoreBadgesProps) {
  // ЗАМЕНИТЕ ЭТИ ССЫЛКИ НА ВАШИ РЕАЛЬНЫЕ ССЫЛКИ!
  const appStoreUrl = "https://apps.apple.com/app/nomad-pays/idXXXXXXXXXX";
  const googlePlayUrl = "https://play.google.com/store/apps/details?id=com.nomadpays.app";

  return (
    <div className={`flex flex-col sm:flex-row gap-3 justify-center items-center ${className}`}>
      <Link
        href={appStoreUrl}
        target="_blank"
        rel="noopener noreferrer"
        className="inline-flex items-center justify-center bg-black text-white rounded-xl px-6 py-3 hover:bg-neutral-800 transition-all duration-300 shadow-lg hover:shadow-xl min-w-[160px]"
        aria-label="Download on the App Store"
      >
        <svg className="w-6 h-6 mr-2" fill="currentColor" viewBox="0 0 24 24">
          <path d="M17.05 20.28c-.98.95-2.05.88-3.08.4-1.09-.5-2.08-.48-3.24 0-1.44.62-2.2.44-3.06-.4C2.79 15.25 3.51 7.59 9.05 7.31c1.35.07 2.29.74 3.08.8 1.18-.24 2.31-.93 3.57-.84 1.51.12 2.65.72 3.4 1.8-3.12 1.87-2.38 5.98.48 7.13-.57 1.5-1.31 2.99-2.54 4.09l.01-.01zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z"/>
        </svg>
        <div className="flex flex-col items-start">
          <span className="text-xs leading-none">Download on the</span>
          <span className="text-lg font-semibold leading-tight">App Store</span>
        </div>
      </Link>
      
      <Link
        href={googlePlayUrl}
        target="_blank"
        rel="noopener noreferrer"
        className="inline-flex items-center justify-center bg-black text-white rounded-xl px-6 py-3 hover:bg-neutral-800 transition-all duration-300 shadow-lg hover:shadow-xl min-w-[160px]"
        aria-label="Get it on Google Play"
      >
        <svg className="w-6 h-6 mr-2" fill="currentColor" viewBox="0 0 24 24">
          <path d="M3,20.5V3.5C3,2.91 3.34,2.39 3.84,2.15L13.69,12L3.84,21.85C3.34,21.6 3,21.09 3,20.5M16.81,15.12L6.05,21.34L14.54,12.85L16.81,15.12M20.16,10.81C20.5,11.08 20.75,11.5 20.75,12C20.75,12.5 20.53,12.9 20.18,13.18L17.89,14.5L15.39,12L17.89,9.5L20.16,10.81M6.05,2.66L16.81,8.88L14.54,11.15L6.05,2.66Z"/>
        </svg>
        <div className="flex flex-col items-start">
          <span className="text-xs leading-none">Get it on</span>
          <span className="text-lg font-semibold leading-tight">Google Play</span>
        </div>
      </Link>
    </div>
  );
}
```

**Что изменилось:**
- Добавлены переменные `appStoreUrl` и `googlePlayUrl` в начале компонента
- Ссылки `href="#"` заменены на реальные ссылки
- Добавлены атрибуты `target="_blank"` и `rel="noopener noreferrer"` для открытия в новой вкладке

### 7.3. Настройка ссылок через переменные окружения (рекомендуется)

Для более гибкой настройки можно использовать переменные окружения:

1. В файле `components/AppStoreBadges.tsx` замените строки с URL на:

```typescript
const appStoreUrl = process.env.NEXT_PUBLIC_APP_STORE_URL || "https://apps.apple.com/app/nomad-pays/idXXXXXXXXXX";
const googlePlayUrl = process.env.NEXT_PUBLIC_GOOGLE_PLAY_URL || "https://play.google.com/store/apps/details?id=com.nomadpays.app";
```

2. В Vercel Dashboard → **Settings → Environment Variables** добавьте:
   - **Name**: `NEXT_PUBLIC_APP_STORE_URL`
   - **Value**: ваша ссылка на App Store
   - **Environment**: Production, Preview, Development

   - **Name**: `NEXT_PUBLIC_GOOGLE_PLAY_URL`
   - **Value**: ваша ссылка на Google Play
   - **Environment**: Production, Preview, Development

**Преимущества**: Можно менять ссылки без изменения кода, просто обновив переменные окружения.

### 7.4. Если приложение еще не опубликовано

Если приложение еще не опубликовано в магазинах:

1. **Временно** используйте ссылки на страницы ожидания:
   - App Store: `https://apps.apple.com/app/nomad-pays/idXXXXXXXXXX` (замените на реальный ID после публикации)
   - Google Play: `https://play.google.com/store/apps/details?id=com.nomadpays.app` (замените на реальный package name)

2. Или создайте страницы-заглушки:
   - `/coming-soon-ios` — для iOS
   - `/coming-soon-android` — для Android
   - И используйте эти ссылки в кнопках

3. После публикации обновите ссылки на реальные.

### 7.5. Тестирование кнопок

1. Сохраните изменения в `components/AppStoreBadges.tsx`
2. Закоммитьте и запушьте:
```bash
git add components/AppStoreBadges.tsx
git commit -m "Update App Store and Google Play links"
git push
```

3. После деплоя на Vercel:
   - Откройте главную страницу сайта
   - Нажмите на кнопку **"Download on the App Store"** — должна открыться страница приложения в App Store
   - Нажмите на кнопку **"Get it on Google Play"** — должна открыться страница приложения в Google Play
   - Проверьте, что ссылки открываются в новой вкладке

### 7.6. Изменение текста кнопок (локализация)

Если нужно изменить текст кнопок в зависимости от языка:

1. Добавьте переводы в `messages/ru.json` и `messages/en.json`:

```json
// messages/en.json
{
  "common": {
    "appStore": {
      "download": "Download on the",
      "store": "App Store"
    },
    "googlePlay": {
      "getIt": "Get it on",
      "store": "Google Play"
    }
  }
}

// messages/ru.json
{
  "common": {
    "appStore": {
      "download": "Скачать в",
      "store": "App Store"
    },
    "googlePlay": {
      "getIt": "Доступно в",
      "store": "Google Play"
    }
  }
}
```

2. Обновите компонент `AppStoreBadges.tsx` для использования переводов:

```typescript
import { getTranslations } from "@/lib/i18n";

export default function AppStoreBadges({ locale, className = "" }: AppStoreBadgesProps) {
  const t = getTranslations(locale);
  
  // ... остальной код ...
  
  <span className="text-xs leading-none">{t.common.appStore.download}</span>
  <span className="text-lg font-semibold leading-tight">{t.common.appStore.store}</span>
  
  // ... и для Google Play ...
}
```

### 7.7. Изменение дизайна кнопок

Если нужно изменить внешний вид кнопок:

1. **Цвет фона**: Измените `bg-black` на другой цвет (например, `bg-accent-darkBlue`)
2. **Размер**: Измените `px-6 py-3` на другие значения
3. **Иконки**: Замените SVG на другие иконки или используйте изображения
4. **Скругление**: Измените `rounded-xl` на `rounded-lg` или `rounded-full`

Пример изменения цвета:
```typescript
className="... bg-accent-darkBlue ... hover:bg-primary-700 ..."
```

---

## 🐛 Решение проблем

### Проблема: Сборка падает с ошибкой

**Решение**: Проверьте логи сборки в Vercel Dashboard. Частые причины:
- Ошибки в коде
- Отсутствующие зависимости
- Неправильные переменные окружения

### Проблема: Домен не привязывается

**Решение**: 
1. Убедитесь, что DNS записи добавлены правильно
2. Подождите до 48 часов для распространения DNS
3. Проверьте, что домен не привязан к другому сервису

### Проблема: Сайт работает, но стили не применяются

**Решение**: Убедитесь, что в `next.config.js` правильно настроены пути и нет конфликтов с конфигурацией.

### Проблема: Форма не отправляет email

**Решение**: 
1. Проверьте, что `RESEND_API_KEY` добавлен в Environment Variables в Vercel
2. Убедитесь, что пакет `resend` установлен (`npm install resend`)
3. Проверьте, что код отправки email обновлен в `app/api/contact/route.ts` (не закомментирован)
4. Проверьте логи в Vercel Dashboard → **Deployments** → выберите деплой → **Functions** → `api/contact`
5. Убедитесь, что адрес получателя правильный: `ads@nomadplatforms.co.uk`
6. Проверьте лимиты Resend (бесплатный план: 100 писем/день)

### Проблема: Кнопки App Store/Google Play не работают

**Решение**:
1. Убедитесь, что ссылки обновлены в `components/AppStoreBadges.tsx`
2. Проверьте, что ссылки начинаются с `https://`
3. Убедитесь, что приложение опубликовано в магазинах
4. Проверьте, что ссылки открываются в новой вкладке (должны быть `target="_blank"`)

### Проблема: Email отправляется, но не приходит

**Решение**:
1. Проверьте папку **Спам** в почтовом ящике `ads@nomadplatforms.co.uk`
2. Убедитесь, что домен верифицирован в Resend (если используете свой домен)
3. Проверьте логи Resend в Dashboard → **Logs**
4. Убедитесь, что адрес отправителя правильный (для верифицированного домена)

---

## 📚 Полезные ссылки

- [Документация Vercel](https://vercel.com/docs)
- [Документация Next.js](https://nextjs.org/docs)
- [Документация Resend](https://resend.com/docs)
- [Настройка DNS на Vercel](https://vercel.com/docs/concepts/projects/domains)

---

## ✅ Чек-лист после деплоя

- [ ] Сайт доступен по домену
- [ ] Все страницы работают корректно
- [ ] Переключение языков работает
- [ ] Кнопки App Store и Google Play ведут на правильные страницы
- [ ] Форма контактов отправляет email на `ads@nomadplatforms.co.uk`
- [ ] Email приходит в правильном формате с HTML разметкой
- [ ] SSL сертификат установлен (зеленый замок в браузере)
- [ ] Аналитика подключена (если настроено)
- [ ] Все ссылки работают
- [ ] Мобильная версия корректно отображается
- [ ] Кнопки App Store/Google Play открываются в новой вкладке

---

**Готово! 🎉 Ваш сайт размещен на Vercel и доступен по вашему домену!**
