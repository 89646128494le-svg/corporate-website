# 📤 Инструкция по обновлению репозитория на GitHub

## 🔄 Обновление существующего репозитория

Для обновления репозитория [https://github.com/89646128494le-svg/corporate-website](https://github.com/89646128494le-svg/corporate-website) выполните следующие команды:

### Вариант 1: Если Git уже инициализирован в проекте

```powershell
# 1. Перейдите в папку проекта
cd "C:\Users\Lev\Desktop\Проекты\corporate website"

# 2. Проверьте текущий remote (если нужно обновить URL)
git remote -v

# 3. Если URL неправильный, удалите старый и добавьте новый:
git remote remove origin
git remote add origin https://github.com/89646128494le-svg/corporate-website.git

# 4. Проверьте статус изменений
git status

# 5. Добавьте все изменения
git add .

# 6. Создайте коммит с описанием изменений
git commit -m "Update: Add App Store badges, contact form, email integration, deployment instructions"

# 7. Отправьте на GitHub
git push origin main
```

### Вариант 2: Если Git еще не инициализирован в проекте

```powershell
# 1. Перейдите в папку проекта
cd "C:\Users\Lev\Desktop\Проекты\corporate website"

# 2. Инициализируйте Git (если еще не сделано)
git init

# 3. Переименуйте ветку в main (если нужно)
git branch -M main

# 4. Подключите удаленный репозиторий
git remote add origin https://github.com/89646128494le-svg/corporate-website.git

# 5. Добавьте все файлы
git add .

# 6. Создайте первый коммит
git commit -m "Initial commit: Nomad Pays corporate website with all features"

# 7. Отправьте на GitHub
git push -u origin main
```

### Вариант 3: Если на GitHub уже есть код и нужно синхронизировать

```powershell
# 1. Перейдите в папку проекта
cd "C:\Users\Lev\Desktop\Проекты\corporate website"

# 2. Получите изменения с GitHub (если есть)
git pull origin main --allow-unrelated-histories

# 3. Если возникли конфликты, разрешите их, затем:
git add .
git commit -m "Merge remote changes"

# 4. Отправьте ваши изменения
git push origin main
```

## 📋 Что будет обновлено

После выполнения команд на GitHub будет загружено:

- ✅ **Новые компоненты:**
  - `components/AppStoreBadges.tsx` — кнопки App Store и Google Play
  - `components/AdvertiserForm.tsx` — форма для рекламодателей
  
- ✅ **API Route:**
  - `app/api/contact/route.ts` — отправка email через Resend

- ✅ **Обновленные файлы:**
  - `app/[locale]/page.tsx` — добавлены кнопки App Store в Hero
  - `components/Footer.tsx` — добавлены кнопки App Store в Footer
  - `app/[locale]/for-advertisers/page.tsx` — интегрирована форма контактов
  - `messages/ru.json` и `messages/en.json` — добавлены переводы для формы

- ✅ **Документация:**
  - `INSTRUCTION.md` — подробная инструкция по деплою и настройке
  - `README.md` — обновленная документация проекта

## ⚠️ Важные замечания

1. **Убедитесь, что `.gitignore` правильно настроен** — он должен исключать `node_modules/`, `.next/`, `.env.local` и другие служебные файлы.

2. **Не коммитьте секреты** — переменные окружения (API ключи, пароли) должны быть только в `.env.local`, который добавлен в `.gitignore`.

3. **Проверьте перед push:**
   ```powershell
   git status
   git diff  # посмотреть изменения
   ```

## 🐛 Решение проблем

### Проблема: "fatal: not a git repository"

**Решение:** Git не инициализирован. Выполните:
```powershell
git init
git branch -M main
```

### Проблема: "remote origin already exists"

**Решение:** Удалите старый remote и добавьте новый:
```powershell
git remote remove origin
git remote add origin https://github.com/89646128494le-svg/corporate-website.git
```

### Проблема: "failed to push some refs"

**Решение:** Сначала получите изменения с GitHub:
```powershell
git pull origin main --allow-unrelated-histories
# Разрешите конфликты, если есть
git push origin main
```

### Проблема: "authentication failed"

**Решение:** Используйте Personal Access Token (PAT):
1. Перейдите в GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Создайте новый token с правами `repo`
3. Используйте token вместо пароля при push

Или используйте GitHub CLI:
```powershell
gh auth login
```

## ✅ После успешного обновления

1. Проверьте, что все файлы на GitHub:
   - Откройте https://github.com/89646128494le-svg/corporate-website
   - Убедитесь, что видны новые файлы и изменения

2. Если проект подключен к Vercel:
   - Vercel автоматически запустит новый деплой
   - Проверьте статус в Vercel Dashboard

3. Проверьте работу сайта:
   - Откройте ваш сайт на Vercel
   - Убедитесь, что все изменения работают корректно

---

**Готово! 🎉 Ваш репозиторий обновлен на GitHub!**
